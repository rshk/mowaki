export const API_URL = import.meta.env.VITE_API_URL;

// Keys to store the session ID in localStorage / sessionStorage
export const SESSION_ID_KEY = import.meta.env.VITE_SESSION_ID_KEY || "session-token";
